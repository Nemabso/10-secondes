//import { SET_PLAYERS_OK } from "../Actions/playActions";

// init state for game
const initState = {
  play: false,
  playersok: [],
};

// the sign up reducer
const gameReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default gameReducer;
